# To run this application

```bash
git clone https://github.com/Accumulative/MachineLearning
cd MachineLearning
virtualenv venv
source venv/bin/activate
pip install -r requirements.txt
python src/api/app.py
```

Open a new terminal
```bash
cd MachineLearning
cd www
npm install
npm start
```